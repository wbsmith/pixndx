/**
 * Stub file for production builds.
 * 
 * In production, image data comes from DynamoDB/S3 via Amplify Data API.
 * This stub exists so TypeScript can resolve the import.
 */

import type { ImageMetadata } from '@/types/gallery';

export const localImages: ImageMetadata[] = [];

