# PixNdx Gallery Preprocessing Pipeline
# Python 3.10+ required

# Image processing
Pillow>=10.0.0
imagehash>=4.3.1          # Perceptual hashing for duplicate detection
requests>=2.31.0          # HTTP client for Ollama

# ML/Embeddings
numpy>=1.24.0
torch>=2.0.0              # PyTorch for CLIP
torchvision>=0.15.0       # Image transforms
sentence-transformers>=2.2.0  # CLIP embeddings (clip-ViT-B-32)
transformers>=4.30.0      # Hugging Face transformers

# Similarity & Clustering
scikit-learn>=1.3.0       # Cosine similarity, basic clustering
faiss-cpu>=1.7.4          # Fast approximate nearest neighbor (use faiss-gpu if CUDA available)
hdbscan>=0.8.33           # Density-based clustering
umap-learn>=0.5.4         # Dimensionality reduction for visualization
networkx>=3.1             # Graph algorithms (Louvain community detection)
python-louvain>=0.16      # Louvain algorithm implementation (community)

# Color analysis
colorgram.py>=1.2.0       # Color palette extraction
colormath>=3.0.0          # Color space conversions

# Utilities
tqdm>=4.65.0              # Progress bars
click>=8.1.0              # CLI framework
pyyaml>=6.0               # Config files
orjson>=3.9.0             # Fast JSON serialization


